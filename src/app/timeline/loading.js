import React from 'react';
import { BounceLoader } from 'react-spinners';

const loading = () => {
    return (
        <div>
            <BounceLoader />
        </div>
    );
};

export default loading;